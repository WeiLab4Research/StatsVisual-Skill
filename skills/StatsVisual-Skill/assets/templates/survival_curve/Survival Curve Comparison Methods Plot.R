## 生存曲线比较

存在多条生存曲线时，需要比较存曲线之间的差异有无统计学意义。应根据曲线特征，选择合适的方法(图\@ref(fig:survival-compare))[@RN12]。

```{r survival-compare, fig.width=4, fig.height=8, fig.cap = "多种方法的生存曲线比较结果"}

# source("rcode/libraries.r")
# source("rcode/surv2sample_local.r")
# # 自定义的KM曲线比较函数
# source("rcode/KM_compare.r")


# init(journal_theme = "jco")
# 
# fig.compare = km_compare(fit2, ds2)
# 
# fig.compare

```

